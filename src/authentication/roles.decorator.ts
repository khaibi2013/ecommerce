// // roles.decorator.ts

// import { SetMetadata } from '@nestjs/common';
// import { UserRole } from '../database/role.enum';

// export const Roles = (...roles: UserRole[]) => SetMetadata('role', roles);
